import { createTheme } from "@mui/material/styles"

// Neutral MUI defaults for now — swap palette/typography here once real
// branding is decided. Nothing else in the app should import from
// @mui/material/styles directly; this is the one place theme lives.
export const theme = createTheme()
